{
  "private": true,
  "packageManager": "yarn@1.22.17",
  "workspaces": [
    "app",
    "cms"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel --no-cache",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.1.3"
  }
}
